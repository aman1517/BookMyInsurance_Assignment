import React from "react";

const CarDetails = () => {
  let data = JSON.parse(localStorage.getItem("localSData"));
  console.log(data);

  return (
    <div>
      <div></div>
    </div>
  );
};

export default CarDetails;
